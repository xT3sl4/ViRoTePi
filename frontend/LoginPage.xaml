<Window x:Class="frontend.LoginPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:frontend"
        mc:Ignorable="d"
        Title="LoginPage" Height="550" Width="350">
    <Grid>
        <Border Width="300"
                Height="480" 
                Background="White"
                CornerRadius="6">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15"
                                  Direction ="-90" 
                                  RenderingBias ="Quality" 
                                  ShadowDepth ="2" 
                                  Color ="Gray" />
            </Border.Effect>

            <StackPanel Orientation="Vertical" Margin="20">
                <TextBlock Text="Login" 
                           FontSize="25"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Top"
                           FontWeight="Bold"
                           Padding="10"/>

                <Grid VerticalAlignment="Center">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Username" Grid.Row="0" Margin="0,15,0,5" HorizontalAlignment="Left"/>
                    <TextBox x:Name="txtUsername"
                             Grid.Row="1"
                             Padding="0,5"
                             Width="255"
                             BorderThickness="0,0,0,1"
                             HorizontalAlignment="Left"/>

                    <TextBlock Text="Password" Grid.Row="2" Margin="0,15,0,5" HorizontalAlignment="Left"/>
                    <PasswordBox x:Name="txtPassword"
                                 Grid.Row="3"
                                 Padding="0,5"                               
                                 Width="255"
                                 BorderThickness="0,0,0,1"
                                 HorizontalAlignment="Left"/>
                </Grid>

                <TextBlock Text="forgot password?" 
                           Margin="0,10,0,0"
                           HorizontalAlignment="Right" 
                           FontSize="11" 
                           Foreground="Gray" 
                           Cursor="Hand"/>

                <Button x:Name="LoginBtn" 
                        Content="Login" 
                        Foreground="White"
                        Background="Black"
                        Margin="0,20,0,10"
                        Height="35"
                        Width="250" 
                        Click="LoginBtn_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border x:Name="LoginBorder" Background="{TemplateBinding Background}" CornerRadius="16">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#333333" TargetName="LoginBorder" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>

                <Grid Margin="0,5">
                    <Separator Width="100" HorizontalAlignment="Left" Background="#EEE"/>
                    <TextBlock Text="lub" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="10" Foreground="Gray"/>
                    <Separator Width="100" HorizontalAlignment="Right" Background="#EEE"/>
                </Grid>

                <Button x:Name="GoogleLoginBtn" 
                        Click="LoginGoogle_Click"
                        Margin="0,10"
                        Height="40"
                        Width="250"
                        Background="White"
                        BorderBrush="#DDDDDD"
                        BorderThickness="1"
                        Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="GoogleBorder" 
                                                Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="16">
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                <Image Source="https://upload.wikimedia.org/wikipedia/commons/c/c1/Google_Logo.svg" 
                                                       Width="16" Height="16" Margin="0,0,10,0"/>
                                                <TextBlock Text="Zaloguj przez Google" 
                                                           VerticalAlignment="Center" 
                                                           FontSize="12" 
                                                           FontWeight="SemiBold" 
                                                           Foreground="#555"/>
                                            </StackPanel>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#F9F9F9" TargetName="GoogleBorder"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>

                <TextBlock Text="Sign Up"
                           Margin="0,15"
                           Foreground="Gray"
                           VerticalAlignment="Bottom"
                           HorizontalAlignment="Center"
                           Cursor="Hand"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>